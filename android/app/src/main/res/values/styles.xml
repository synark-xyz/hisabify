<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Dark brand background replaces the default system colour -->
        <item name="windowSplashScreenBackground">#080c14</item>
        <!-- Animated logo (zoom-out) shown as the splash icon (Android 12+) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_splash_logo</item>
        <!-- Transition to the normal no-action-bar theme after the splash -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <!-- Pre-Android-12 compat: window background before first frame -->
        <item name="android:windowBackground">@drawable/splash</item>
    </style>
</resources>